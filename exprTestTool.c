#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#define INT_MAX_LENGTH 12

struct Fact{
	(int)(*computeFactorial)(void *, int);
};
		int computeFactorial (int num) {
				if(num < 1) {
						num_aux = 1;
				} else {
						num_aux = num;
				}
		}

void * new(int type){
	void * object;
	switch(type){
		default:
			return NULL;
	}
}

int main(void){
	return 0;
}